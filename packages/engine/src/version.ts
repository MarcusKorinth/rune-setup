/**
 * Version of the engine package. Pinned to `packages/engine/package.json` by
 * `tests/package-versions.test.ts`, so a release bump cannot land here half-done.
 */
export const RUNE_VERSION = '0.1.0';
