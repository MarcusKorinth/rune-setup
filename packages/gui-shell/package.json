{
  "name": "@rune/gui-shell",
  "version": "0.1.0",
  "private": true,
  "description": "RUNE GUI shell — Electron wizard; main process hosts the engine, renderer is a pure renderer",
  "license": "MIT",
  "type": "module",
  "main": "./dist/main/index.js",
  "engines": {
    "node": ">=24"
  },
  "dependencies": {
    "@rune/engine": "0.1.0"
  },
  "scripts": {
    "build": "tsc -b",
    "prepare:electron": "install-electron",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "~1.63.0",
    "electron": "^44.2.0"
  }
}
