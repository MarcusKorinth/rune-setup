{
  "name": "@rune/cli",
  "version": "0.0.0",
  "description": "rune — the RUNE command line (guided and automated runs, validate, schema)",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "rune": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@rune/engine": "0.0.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "zod": "^4.4.3"
  },
  "scripts": {
    "build": "tsc -b",
    "prepack": "npm run build"
  }
}
