{
  "name": "@rune/cli",
  "version": "0.1.0",
  "description": "rune — interactive, non-interactive, and GUI runs; GUI shell install, manifest validation, and schema inspection",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "bin": {
    "rune": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@rune/engine": "0.1.0",
    "commander": "^15.0.0"
  },
  "devDependencies": {
    "zod": "^4.5.4"
  },
  "scripts": {
    "build": "tsc -b",
    "prepack": "node ../../scripts/clean-package.mjs && npm run build"
  }
}
