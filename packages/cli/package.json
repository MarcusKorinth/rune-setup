{
  "name": "@rune/cli",
  "version": "0.1.0",
  "description": "rune — interactive, non-interactive, and GUI runs; GUI shell install, manifest validation, and schema inspection",
  "license": "MIT",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "bin": {
    "rune": "./dist/main.js"
  },
  "files": [
    "dist"
  ],
  "publishConfig": {
    "access": "public"
  },
  "dependencies": {
    "@rune/engine": "0.1.0",
    "commander": "^14.0.3"
  },
  "devDependencies": {
    "zod": "^4.4.3"
  },
  "scripts": {
    "build": "tsc -b",
    "prepack": "npm run build"
  }
}
